<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dlb.bim.dao.IIfcObjectDao">
    <insert id="insertIfcStoreModel" keyProperty="gid" parameterType="IfcStoreModel">
		insert into data.ifc_store_model
			(gid,ifc_object_bytes)
			values
			(#{gid},#{ifcObjectBytes})
	</insert>
</mapper>
